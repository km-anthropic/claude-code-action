// Test file for auto-fix CI workflow - Round 5
// Testing after fixing prettier warnings on main branch

// Syntax error: missing closing bracket  
const array = [1, 2, 3;

// Type error: wrong return type
function getString(): number {
  return "hello"; // returning string instead of number
}

// Syntax error: missing closing quote
const name = "Alice;

// Undefined function
const value = calculateValue(10);

// Missing closing brace in object
const config = {
  enabled: true,
  timeout: 5000

// Type error: incompatible assignment
const count: string = 42;

export { getString, config };