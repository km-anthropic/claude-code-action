const   unformattedCode    =    "this has weird spacing"
    function   badlyFormatted(  x:number,y:string   ){
return x+y}

const obj={a:1,b:2,c:3,d:4,e:5}
  if(true){console.log("bad formatting")}

    class    MyClass    {
constructor(  ){
}
        method(    ){return"bad"}
}
